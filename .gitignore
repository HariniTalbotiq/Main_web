# OS + editor cruft
.DS_Store
Thumbs.db
desktop.ini
__MACOSX/

# tool state, not source
.impeccable/

# nothing here yet, but keep secrets out by default
.env
.env.*
*.pem
*.key

# agent/tool scratch state, not source
.playwright-mcp/
.probe2/

# Vercel CLI links this checkout to a project; its own README says do not commit it
.vercel/
